{
  "name": "ayusangh",
  "version": "1.0.0",
  "private": true,
  "description": "Digital Healthcare Discovery & Institutional Rating Platform",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "prisma": "^6.6.0"
  },
  "dependencies": {
    "@prisma/client": "^7.6.0"
  }
}
